Repo: Emeal-o/Journal_abel. Work directly on the main branch — no feature branch, no pull request. Commit and push directly to main.

Bug: comparing the live on-screen Stats card to its exported PNG (via dom-to-image-more), the uppercase label text — "NET RR", "NET PIPS", "GRAND TOTAL", "WEEKLY FINALE", and similar section labels — has visibly wide letter-spacing (tracking) live in the browser (e.g. reads like "N E T   P I P S"), which is the intended design. In the exported PNG, that same text renders with tight/normal letter-spacing ("NET PIPS") — the letter-spacing styling is being LOST during export, not exaggerated. Get the direction right: live = wide spacing (correct/intended), export = tight spacing (bug, missing the styling).

Find wherever these label elements get their letter-spacing — search for "letter-spacing", or Tailwind's "tracking-" classes (tracking-wide, tracking-wider, tracking-widest) in stats-card.tsx, analysis-card.tsx, and ledger-sheet.tsx (whichever components render these specific labels). Likely cause: if letter-spacing is applied via a Tailwind utility class rather than an inline style attribute, dom-to-image-more can sometimes fail to capture that computed style when it clones the DOM for the snapshot, since it doesn't always resolve every stylesheet-derived computed style onto the cloned node.

Fix: for every label element in the export path that currently gets its letter-spacing from a Tailwind tracking- class, add the equivalent letter-spacing value as an explicit inline style (e.g. style={{ letterSpacing: '0.1em' }}) alongside the existing class, so it's guaranteed to be present as a computed style dom-to-image-more can read directly, regardless of how the class-based CSS resolves during the clone. Do not remove the Tailwind class — just make sure the same value is also set inline as a safety net for the export path specifically.

After finding and fixing this, restart the frontend, confirm it builds with no errors, then commit and push directly to main. Confirm explicitly: (1) which specific label elements were missing letter-spacing in the export and now have it added inline, (2) describe testing this by exporting a fresh Stats card PNG and confirming "NET RR"/"NET PIPS"/"GRAND TOTAL" now show the same wide letter-spacing in the exported PNG as they do live on screen — not tight/collapsed.