Repo: Emeal-o/Journal_abel. Work directly on the main branch — no feature branch, no pull request. Commit and push directly to main.

In artifacts/trading-journal/src/pages/analysis.tsx, on desktop/wide viewports, the "By Setup Type" and "Post-Loss Performance" tables look visually different from the "Year by Year" table above them — they appear flatter/plainer, missing the same card border, header background shading, and spacing that Year by Year has. On mobile all three look fine and match each other. I want all three tables to look visually identical at every screen size — same wrapper border, same header row background/padding, same row dividers, same font sizing — the only difference between them should be their column contents, never their visual styling.

IMPORTANT CONSTRAINT — do not reintroduce a mobile bug: the "By Setup Type" and "Post-Loss Performance" tables currently use table-layout: fixed with a colgroup (explicit pixel widths on the numeric columns, e.g. 64px/74px/96px) specifically to fix a real mobile bug where long text in the first column previously pushed the table wider than the screen, clipping the numeric columns off-canvas. Do NOT remove table-fixed or the colgroup, and do NOT change to table-layout: auto — that will reintroduce the clipping bug on narrow phones. Only touch the wrapper div's classes (border, rounded corners, background), the header row's classes (background shading, padding, font weight/color), and row divider classes — purely cosmetic CSS classes, not the table's layout/column-width mechanism.

Match Year by Year's exact Tailwind classes for: the outer wrapper div, the <thead> row, the <th> cells, and the row divider between <tr> elements. Leave column widths, table-fixed, and colgroup on the other two tables completely untouched.

After finishing, restart the frontend, confirm it builds with no errors, then commit and push directly to main. Confirm explicitly: (1) all three tables now look visually identical in wrapper/header/row styling on both mobile and desktop widths, (2) By Setup Type and Post-Loss Performance still use table-fixed with their existing colgroup pixel widths — unchanged, (3) no clipping/overflow reappears on a narrow mobile width for either of those two tables.